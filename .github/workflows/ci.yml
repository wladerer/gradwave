name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.12

      - name: Sync dependencies
        run: uv sync --all-extras

      # Non-blocking for now: main carries pre-existing findings. Flip to a
      # hard gate (drop continue-on-error) once they are cleaned up.
      - name: Ruff
        continue-on-error: true
        run: uv run ruff check

      - name: Standard test tier
        run: uv run pytest -m "not slow and not torture and not gpu"
