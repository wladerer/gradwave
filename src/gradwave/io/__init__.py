"""I/O layer: run reporting, checkpoints, run metadata, input templates,
and the pandas/matplotlib analysis helpers (the `analysis` extra).

Subpackage convention: import from the leaf module (gradwave.io.checkpoint,
gradwave.io.analysis, ...); this __init__ stays empty.
"""
