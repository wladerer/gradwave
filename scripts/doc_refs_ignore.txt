# Allow-list for scripts/check_doc_refs.py — repo-relative path references that
# are intentionally absent from a clean checkout (generated/local artifacts, or
# provenance notes about outputs that are not committed). One path per line;
# '#' starts a comment. Prefer FIXING a stale reference over ignoring it; add an
# entry here only when the reference is deliberately to a non-tracked file.

# Provenance note in benchmarks/pulay_accuracy/RESULTS.md: the geometry came from
# a local relax run whose output under benchmarks/results/ is generated, not
# committed.
benchmarks/results/sio2-elastic/out_relax/relax.json

# docs/h100_backlog.md forward-references. The BUILD-FIRST items name the driver
# path they will land at once built (TODO markers in the doc); the other two live
# on named branches (feat/h100-test-plan, worktree-phonon-config-batching), not on
# main. All are intentional pointers, not stale references.
benchmarks/h100/H100_TEST_PLAN.md
benchmarks/phonon_batch/config_batch_probe.py
experiments/surface_efficiency/vacuum_ladder.py
experiments/surface_efficiency/tt_rank.py
experiments/stochastic_dft/variance_probe.py
experiments/dmi/fege_full_vector.py
